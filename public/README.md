# public/

Statische Assets, die Vite unverändert unter `/` ausliefert.

## mockup-bg.jpg  (vom Nutzer bereitzustellen)

Das Hintergrund-/Mockup-Bild für die Live-Vorschau erwartet die App unter:

    public/mockup-bg.jpg

Einfach die gewünschte Bilddatei genau so benennen und hier ablegen.
Fehlt die Datei, zeigt die Vorschau einen grauen Fallback-Hintergrund.
